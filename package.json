{
  "name": "tabby-agent-chat",
  "version": "0.1.0",
  "description": "Agent Chat for Tabby — exposes terminal tabs over MCP so an AI agent in one tab can send input to another",
  "keywords": ["tabby-plugin"],
  "author": "tabby-agent-chat contributors",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "../tabby/node_modules/.bin/webpack --progress --color",
    "watch": "../tabby/node_modules/.bin/webpack --progress --color --watch"
  },
  "files": ["dist"]
}
